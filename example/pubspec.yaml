name: example
description: A pro_pretty_logging example app.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  pro_pretty_logging:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  pro_lints: ^3.0.0

flutter:
  uses-material-design: true
